# Исходники и тесты
src/**
test/**
**/*.ts
**/*.tsx

# Конфигурация разработки
tsconfig.json
esbuild.config.mjs
.vscode/**
.gitignore
.cursor/**
cliff.toml

# Публикация в MCP Registry и npm (не нужны в VSIX)
server.json
run-mcp.js

# Скрипты и прочее
scripts/**
**/*.map

# CI
.github/**

# Скомпилированные тесты (не нужны в VSIX)
out/test/**

# Зависимости (MCP-сервер собран в бандл out/src/index.js; extension.js использует только vscode)
node_modules/**

# Локальные и временные
*.log
.DS_Store
